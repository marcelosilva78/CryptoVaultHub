// SPDX-License-Identifier: MIT
pragma solidity 0.8.27;

/**
 * @notice A contract that rejects all ETH transfers (no receive/fallback).
 */
contract ETHRejecter {
    // Intentionally no receive() or fallback()
}
