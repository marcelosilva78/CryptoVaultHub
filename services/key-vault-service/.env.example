# Key Vault Service Configuration
PORT=3005
DATABASE_URL=mysql://root:password@localhost:3306/cvh_keyvault

# CRITICAL: Use a strong, random password in production (64+ characters)
VAULT_MASTER_PASSWORD=CHANGE_ME_TO_A_STRONG_RANDOM_PASSWORD

# PBKDF2 iterations for KEK derivation (OWASP 2024: 600000 for PBKDF2-SHA256)
KDF_ITERATIONS=600000

# Shared secret for authenticating internal service-to-service calls
INTERNAL_SERVICE_KEY=change-me-to-random-secret

# mTLS server configuration (set to true in production)
# When enabled, key-vault-service requires client certificates from callers
VAULT_TLS_ENABLED=false
VAULT_TLS_CERT_PATH=/tls/server.crt
VAULT_TLS_KEY_PATH=/tls/server.key
VAULT_TLS_CA_PATH=/tls/ca.crt
